<div class="row article">
  <div class="col-md-4 col-sm-4">
    <%= image_tag(article.image_url(:thumb)) %>
  </div>
  <div class="meta col-md-6 col-sm-6">
    <h3><%= article.title %></h3>
    <p class="description">
      <%= article.body %>
    </p>
    <%= link_to "Read more...", article.url if article.url.present? %>
  </div>
</div>
